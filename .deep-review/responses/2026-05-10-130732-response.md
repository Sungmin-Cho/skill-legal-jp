# Deep Review Response — legal-jp lookup hardening re-review

Generated: 2026-05-10 13:07:32 KST

## Source Review

Report: `.deep-review/reports/2026-05-10-130732-review.md`

## Response Summary

All four lookup hardening findings were accepted and fixed.

Fix commit:
- `340a858 fix: harden precedent and abbreviation lookup`

## Findings Response

### P1 — Implicit no-decade precedent search could cross the dataset boundary

Decision: Accepted.

Verification:
- Added a regression that symlinks `precedent/2030` to an outside directory and runs a default no-decade title search.

Implementation:
- Validated implicit decade directory names as four digits.
- Resolved each implicit decade directory through `ensure_within(precedent_dir, path)` before returning it.
- Changed symlink escape from silent search success to data error exit `2`.

### P2 — Abbreviation search could skip later required source validation

Decision: Accepted.

Verification:
- Added a regression where `ryakusyou.json` is missing while `--abbr 民法 --limit 1` can be satisfied from an earlier source.

Implementation:
- Removed the early return from abbreviation search.
- Kept result limiting at final output construction, after every required abbreviation source has been opened and validated.

### P1 — Case-number lookup missed common Japanese citation forms

Decision: Accepted.

Verification:
- Added a regression for `令和２年（受）第１２３号` against dataset case number `令和2(受)123`.
- Ran a real-data smoke for `令和５年（受）第６０４号`, which returned `令和5(受)604`.

Implementation:
- Added NFKC normalization for case-number comparisons.
- Removed whitespace, `年`, `第`, and `号` tokens from normalized case-number strings before substring matching.

### P2 — No-decade case-number search eagerly loaded every detail JSON

Decision: Accepted.

Verification:
- Added a regression that corrupts a nonmatching detail file and proves a different matching case-number lookup still succeeds.

Implementation:
- Added a `load_details` switch to metadata iteration.
- Disabled eager detail loading for case-number matching.
- Loaded detail JSON lazily only after a candidate case number matched and before output/court filtering needed merged fields.

## Verification

Commands run after implementation:

```bash
python3 -m unittest tests/test_search_law.py tests/test_search_precedent.py -v
python3 -m py_compile .claude/skills/legal-jp/scripts/search_law.py .claude/skills/legal-jp/scripts/search_precedent.py
git diff --check
python3 .claude/skills/legal-jp/scripts/search_precedent.py --case-number '令和５年（受）第６０４号' --limit 1
```

Results:
- `35` unit tests passed.
- Python compile check passed.
- `git diff --check` passed.
- Real-data case-number smoke returned `令和5(受)604`.
