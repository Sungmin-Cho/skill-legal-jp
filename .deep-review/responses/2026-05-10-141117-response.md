# Deep Review Response — legal-jp edge-case re-review

Generated: 2026-05-10 14:11:17 KST

## Source Review

Report: `.deep-review/reports/2026-05-10-141117-review.md`

## Response Summary

All three findings were accepted and fixed.

Fix commit:
- `8d57961 fix: harden legal search edge cases`

## Finding Responses

### P1 — Streamed `ryakusyou.json` directory escaped fail-closed handling

Decision: Accepted.

Verification:
- Confirmed `stream_json_array()` handled only `PermissionError` around `path.open()`.

Implementation:
- Converted file-open `OSError` to `DataFileError` in both `load_json()` and `stream_json_array()`.
- Added a regression where required `law/ryakusyou.json` is a directory and `--abbr` returns exit code `2` with stdout `[]`.

### P1 — Title search eagerly loaded unrelated precedent details before matching

Decision: Accepted.

Verification:
- Confirmed title search used eager detail loading, unlike case-number search.

Implementation:
- Added `include_orphans` control to metadata iteration.
- Changed title search to process list metadata first, lazily load row details, skip malformed unmatched details while continuing to later valid matches, and scan orphan details separately.
- Preserved fail-closed behavior when a malformed selected detail prevents any valid result.

### P2 — Abbreviation usage evidence was deduplicated only by law number

Decision: Accepted.

Verification:
- Confirmed canonical law hits and unresolved `abbreviation_usage` evidence shared the same law-number dedupe key.

Implementation:
- Added separate dedupe keys: canonical law results by law identity, unresolved usage evidence by source file, law number, chapter, article, and matched abbreviation entry.
- Added regression coverage for two unresolved `報酬等` usages in the same law number.

## Verification

Commands run after implementation:

```bash
python3 -m unittest tests/test_search_law.py tests/test_search_precedent.py -v
python3 -m py_compile .claude/skills/legal-jp/scripts/search_law.py .claude/skills/legal-jp/scripts/search_precedent.py
git diff --check main
python3 .claude/skills/legal-jp/scripts/search_law.py --repo data_set --abbr 報酬等 --limit 3
python3 .claude/skills/legal-jp/scripts/search_precedent.py --repo data_set --title 損害賠償 --decade 2020 --limit 1
```

Results:
- `50` unit tests passed.
- Python compile check passed.
- Current branch/worktree whitespace check passed.
- Real data abbreviation smoke returned unresolved `abbreviation_usage` evidence.
- Real data precedent title smoke returned a 2020 `損害賠償` Supreme Court result.
