# Deep Review Response — legal-jp implementation

Generated: 2026-05-10 12:41:46 KST

## Source Review

Report: `.deep-review/reports/2026-05-10-124146-review.md`

## Response Summary

All four review findings were accepted after code and real-data verification.

Fix commit:
- `0e68295 fix: harden legal-jp evidence search review findings`

## Findings Response

### P1 — Missing required law JSON files could look like successful empty evidence

Decision: Accepted.

Verification:
- Reproduced with an empty `law/` directory: `--name 民法` returned `[]` and exit `0`.

Implementation:
- Added `DataFileError` to `search_law.py`.
- Made required law JSON sources fail closed for name, exact, abbreviation, and yomikae search modes.
- CLI now prints `[]` and exits `2` for missing or malformed required law JSON.
- Added tests for missing `law/list.json`, malformed `law/list.json`, and missing abbreviation source files.

### P1 — Dataset-controlled precedent detail paths could escape the dataset boundary

Decision: Accepted.

Verification:
- `explicit_json_path()` accepted absolute paths and did not enforce that resolved detail paths stay under the decade directory.

Implementation:
- Rejected absolute explicit precedent paths.
- Added containment check requiring resolved explicit detail paths to stay inside the current `precedent/<decade>/` directory.
- Added tests for absolute path rejection and relative `..` escape rejection.

### P2 — Abbreviation search used whole-record substring matching and ranked misleading results first

Decision: Accepted.

Verification:
- Real-data smoke showed `--abbr 民法` and `--abbr 会社法` returning partial or unrelated metadata before the direct law target.

Implementation:
- Added field-aware abbreviation ranking over `abbs`, `name`, `law_name`, `formal`, `title`, and `ryakusyou_lst` fields.
- Added active law exact-name fallback for `--abbr`, so documented direct examples return `民法` / `会社法` first when the query is also a formal law name.
- Added regression assertion that `--abbr 民法` returns `law/list.json` `民法` first while still including abbreviation dataset evidence.

### P2 — Exact repealed law matches could be hidden behind active partial matches

Decision: Accepted.

Verification:
- Real-data smoke showed `過疎地域自立促進特別措置法 --include-repealed --limit 3` returning active partials before the exact repealed law.

Implementation:
- Changed law-name search to collect exact matches across active and repealed sources before partial matches.
- Added regression test for exact repealed law ranking with a small limit.

## Verification

Commands run after implementation:

```bash
python3 -m unittest tests/test_search_law.py tests/test_search_precedent.py -v
python3 -m py_compile .claude/skills/legal-jp/scripts/search_law.py .claude/skills/legal-jp/scripts/search_precedent.py
git diff --check
```

Results:
- `25` unit tests passed.
- Python compile check passed.
- `git diff --check` passed.

Real-data smoke:

```text
abbr_minpo count=5 first=('民法', 'law/list.json', 'active')
abbr_kaisha count=5 first=('会社法', 'law/list.json', 'active')
repealed_exact count=3 first=('過疎地域自立促進特別措置法', 'law/repeal_list.json', 'repealed')
precedent_title count=3 first=('損害賠償等請求事件', 'precedent/2020/list.json', None)
precedent_text count=3 first=('損害賠償等請求事件', 'precedent/2020/list.json', None)
missing-list 2 []
deep-review fix smoke ok
```

Explicit path escape smoke:

```text
absolute escape rc=2 stdout=[]
escape smoke ok
```
