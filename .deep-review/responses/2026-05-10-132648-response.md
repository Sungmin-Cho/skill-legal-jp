# Deep Review Response — legal-jp empty-query re-review

Generated: 2026-05-10 13:26:48 KST

## Source Review

Report: `.deep-review/reports/2026-05-10-132648-review.md`

## Response Summary

Both empty-query findings were accepted and fixed.

Fix commit:
- `e035e23 fix: reject empty legal lookup queries`

## Findings Response

### P1 — Empty law queries returned unrelated statutes

Decision: Accepted.

Verification:
- Reproduced empty law CLI queries returning real results before the fix.

Implementation:
- Added `require_nonempty_query()` to reject normalized-empty law queries.
- Applied it before dispatching `--name`, `--exact`, `--abbr`, and `--yomikae`.
- Added unit coverage for empty and whitespace-only law query modes.

### P1 — Empty precedent queries returned arbitrary cases

Decision: Accepted.

Verification:
- Reproduced empty precedent CLI queries returning real 2020 precedent results before the fix.

Implementation:
- Added text and case-number non-empty validators.
- Applied them before dispatching `--title`, `--case-number`, and `--text`.
- For case-number validation, punctuation-only legal citation tokens such as `年`, `第`, `号`, and parentheses normalize to empty and fail closed.
- Added unit coverage for empty and whitespace-only precedent query modes.

## Verification

Commands run after implementation:

```bash
python3 -m unittest tests/test_search_law.py tests/test_search_precedent.py -v
python3 -m py_compile .claude/skills/legal-jp/scripts/search_law.py .claude/skills/legal-jp/scripts/search_precedent.py
git diff --check main
python3 .claude/skills/legal-jp/scripts/search_law.py --name '' --limit 1
python3 .claude/skills/legal-jp/scripts/search_law.py --yomikae '   ' --limit 1
python3 .claude/skills/legal-jp/scripts/search_precedent.py --title '' --decade 2020 --limit 1
python3 .claude/skills/legal-jp/scripts/search_precedent.py --case-number ' 年 第 号 （ ） ' --decade 2020 --limit 1
python3 .claude/skills/legal-jp/scripts/search_precedent.py --text '' --decade 2020 --limit 1
```

Results:
- `42` unit tests passed.
- Python compile check passed.
- Current branch/worktree whitespace check passed.
- Empty-query smoke checks returned `[]` with exit code `2`.
