# Deep Review Response — legal-jp precedent fail-closed re-review

Generated: 2026-05-10 14:17:01 KST

## Source Review

Report: `.deep-review/reports/2026-05-10-141701-review.md`

## Response Summary

Both findings were accepted and fixed.

Fix commit:
- `4ae2a91 fix: fail closed on precedent detail read errors`

## Finding Responses

### P2 — Precedent JSON open errors could escape fail-closed handling

Decision: Accepted.

Verification:
- Confirmed `search_precedent.py` converted only `PermissionError` from `load_json()`, unlike the updated law search loader.

Implementation:
- Converted all `OSError` file-open failures in `search_precedent.py::load_json()` into `DataFileError`.
- Added a regression where required `precedent/2020/list.json` is a directory and the CLI returns exit code `2` with stdout `[]`.

### P1 — Title search could hide detail read failures behind successful partial results

Decision: Accepted.

Verification:
- Confirmed title search collected detail-loading errors and raised them only when the result set was empty.

Implementation:
- Split title search into a metadata-first pass and a detail-scan pass.
- Metadata-first results can satisfy `--limit` without touching unrelated detail files.
- If the detail-scan pass is needed, any detail `DataFileError` now fails closed immediately instead of being omitted behind a successful partial result.
- Added regressions for both boundaries: metadata limit-1 result avoids unrelated malformed detail, while incomplete result sets fail closed on corrupt detail.

## Verification

Commands run after implementation:

```bash
python3 -m unittest tests/test_search_law.py tests/test_search_precedent.py -v
python3 -m py_compile .claude/skills/legal-jp/scripts/search_law.py .claude/skills/legal-jp/scripts/search_precedent.py
git diff --check main
```

Results:
- `52` unit tests passed.
- Python compile check passed.
- Current branch/worktree whitespace check passed.
