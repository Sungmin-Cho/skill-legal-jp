# Deep Review Response — legal-jp ryakusyou label re-review

Generated: 2026-05-10 13:48:40 KST

## Source Review

Report: `.deep-review/reports/2026-05-10-134840-review.md`

## Response Summary

The `ryakusyou.json` formal-text matching finding was accepted and fixed.

Fix commit:
- `1127dda fix: restrict ryakusyou abbreviation labels`

## Finding Response

### P2 — `ryakusyou.json` formal text was matched as an abbreviation label

Decision: Accepted.

Verification:
- Reproduced `--abbr 民法 --limit 10` returning usage rows where only `seishiki` mentioned `民法`.

Implementation:
- Restricted `ryakusyou.json` abbreviation matching to label fields: `ryakusyou`, `abb`, and `name`.
- Kept `seishiki` for resolving canonical statute targets after a label has matched.
- Added a regression proving `seishiki: 民法...` alone does not create an abbreviation usage hit for `--abbr 民法`.

## Verification

Commands run after implementation:

```bash
python3 -m unittest tests/test_search_law.py tests/test_search_precedent.py -v
python3 -m py_compile .claude/skills/legal-jp/scripts/search_law.py .claude/skills/legal-jp/scripts/search_precedent.py
git diff --check main
python3 .claude/skills/legal-jp/scripts/search_law.py --abbr 民法 --limit 10
```

Results:
- `46` unit tests passed.
- Python compile check passed.
- Current branch/worktree whitespace check passed.
- Real-data `--abbr 民法 --limit 10` no longer matched `ryakusyou` entries solely through `seishiki`; remaining usage evidence matched label text.
