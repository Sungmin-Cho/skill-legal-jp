# Deep Review Response — legal-jp ryakusyou usage re-review

Generated: 2026-05-10 13:42:02 KST

## Source Review

Report: `.deep-review/reports/2026-05-10-134202-review.md`

## Response Summary

The unresolved `ryakusyou.json` usage finding was accepted and fixed.

Fix commit:
- `a3701d2 fix: preserve unresolved abbreviation usage evidence`

## Finding Response

### P2 — Unresolved `ryakusyou.json` abbreviation usage was silently discarded

Decision: Accepted.

Verification:
- Reproduced `--abbr 報酬等 --limit 5` returning `[]` before the fix.
- Confirmed after the fix that real-data `報酬等` returns `abbreviation_usage` evidence from `law/ryakusyou.json`.

Implementation:
- Added `abbr_usage_entry()` for unresolved `ryakusyou.json` matches.
- Preserved canonical statute hydration when `seishiki` resolves to an exact law name.
- Changed unresolved `ryakusyou.json` matches to return source-backed usage evidence with `status: abbreviation_usage`, host `num`, `chapter`, `ryakusyou_lst`, `abbr_source_file`, and `abbr_raw`.
- Added a regression where the host law exists in `law/list.json` but the `seishiki` phrase is not a statute title, ensuring the result stays usage evidence rather than host statute identity.

## Verification

Commands run after implementation:

```bash
python3 -m unittest tests/test_search_law.py tests/test_search_precedent.py -v
python3 -m py_compile .claude/skills/legal-jp/scripts/search_law.py .claude/skills/legal-jp/scripts/search_precedent.py
git diff --check main
python3 .claude/skills/legal-jp/scripts/search_law.py --abbr 報酬等 --limit 2
```

Results:
- `45` unit tests passed.
- Python compile check passed.
- Current branch/worktree whitespace check passed.
- Real-data `報酬等` returned `abbreviation_usage` records from `law/ryakusyou.json`.
