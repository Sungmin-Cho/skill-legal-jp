# Deep Review Response — legal-jp precedent boundary re-review

Generated: 2026-05-10 12:59:41 KST

## Source Review

Report: `.deep-review/reports/2026-05-10-125941-review.md`

## Response Summary

Both precedent path-boundary findings were accepted and fixed.

Fix commit:
- `a315a6a fix: enforce precedent path boundaries`

## Findings Response

### P1 — Precedent detail discovery could read outside the dataset through symlinks

Decision: Accepted.

Verification:
- Reproduced a symlink under `precedent/2020/` pointing to an outside JSON file and returning that outside content as precedent evidence.

Implementation:
- Applied `ensure_within()` to paths collected by `build_lawsuit_index()`.
- Applied containment checks again before returning discovered lawsuit-id paths and before loading orphan detail files.
- Added regression test for a symlinked discovered detail path escaping the decade directory.

### P1 — `--decade` could escape `precedent/` and search non-precedent JSON

Decision: Accepted.

Verification:
- Reproduced `--decade ../law` returning statute data through the precedent script.

Implementation:
- Restricted explicit `--decade` to four digits.
- Resolved explicit decade paths under `repo/precedent` and required containment.
- Changed missing explicit decade directory from empty success to data error exit `2`.
- Added tests for `--decade ../law` and missing explicit `--decade 2099`.

## Verification

Commands run after implementation:

```bash
python3 -m unittest tests/test_search_law.py tests/test_search_precedent.py -v
python3 -m py_compile .claude/skills/legal-jp/scripts/search_law.py .claude/skills/legal-jp/scripts/search_precedent.py
git diff --check
```

Results:
- `31` unit tests passed.
- Python compile check passed.
- `git diff --check` passed.

Targeted smoke:

```text
['--title', '民法', '--decade', '../law'] 2 []
['--title', '損害賠償', '--decade', '2099'] 2 []
symlink 2 []
precedent path boundary smoke ok
損害賠償等請求事件
民法
```
