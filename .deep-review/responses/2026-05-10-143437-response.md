# Deep Review Response — legal-jp width-normalization re-review

Generated: 2026-05-10 14:34:37 KST

## Source Review

Report: `.deep-review/reports/2026-05-10-143437-review.md`

## Response Summary

Both width-normalization findings were accepted and fixed.

Fix commit:
- `e2c761a fix: normalize full-width legal search text`

## Finding Responses

### P1 — Law abbreviation search missed full-width acronyms typed as ASCII

Decision: Accepted.

Verification:
- Confirmed law search normalization used `casefold()` and whitespace removal without NFKC width folding.

Implementation:
- Updated law search normalization to apply `unicodedata.normalize("NFKC", ...)` before `casefold()`.
- Applied the same normalized comparison to JSON contains search.
- Added regression coverage where ASCII `DV防止法` finds a full-width `ＤＶ防止法` abbreviation.

### P1 — Precedent title/text search missed full-width Latin text typed as ASCII

Decision: Accepted.

Verification:
- Confirmed precedent title, court, and text matching used raw casefold substring matching.

Implementation:
- Added a shared NFKC-normalized `search_key()` path for precedent comparisons.
- Updated `contains()` and snippet lookup to use the normalized search key.
- Added regressions where ASCII `Julius` finds full-width `Ｊｕｌｉｕｓ` in title and text search, including snippet output.

## Verification

Commands run after implementation:

```bash
python3 -m unittest discover -s tests -v
python3 -m py_compile .claude/skills/legal-jp/scripts/search_law.py .claude/skills/legal-jp/scripts/search_precedent.py
git diff --check main
```

Results:
- `57` unit/document tests passed.
- Python compile check passed.
- Current branch/worktree whitespace check passed.
