# Deep Review Report — legal-jp final lookup re-review

Generated: 2026-05-10 13:14:30 KST

## Scope

- Base: `main` (`d4430bed6ccd24117c6f404ccfffcc3454805d76`)
- Head before response: `b25ca30`
- Review target: full branch diff `main..HEAD`

## Reviewers

1. Independent implementation reviewer: `REQUEST_CHANGES`
2. Codex companion review: no actionable correctness issues
3. Codex companion adversarial review: `REQUEST_CHANGES`

## Verdict

`REQUEST_CHANGES`

## Findings

### P1 — Case-number search still loaded unrelated detail JSON during path disambiguation

Source: independent reviewer.

Evidence:
- `.claude/skills/legal-jp/scripts/search_precedent.py` set `load_details=False` for `case_number` search.
- `iter_metadata_entries()` still called `discover_json_path()` for every metadata row.
- For duplicate lawsuit-id candidates, `discover_json_path()` called `detail_matches_entry()`, which loaded candidate detail JSON before the metadata row was known to match the searched case number.

Impact:
- An unrelated malformed detail JSON could make a case-number lookup fail before reaching the actual matching row.

### P2 — Review artifacts failed branch-level `git diff --check`

Source: independent reviewer.

Evidence:
- `git diff --check main...HEAD` reported `new blank line at EOF` in eight earlier `.deep-review` report/response files.

Impact:
- Nonfunctional formatting issue, but it blocks a finish gate that includes branch-level whitespace validation.

### P2 — Case-number normalization missed compact citations without parentheses

Source: Codex companion adversarial review.

Evidence:
- `令和5年(受)第604号` matched real dataset case `令和5(受)604`.
- Compact forms such as `令和5年受第604号` and `令和5受604` still returned no results because normalized comparison retained parenthesis punctuation in the indexed value.

Impact:
- Plausible user-entered Japanese legal citations could miss real Supreme Court precedent evidence.

## Prior Findings Check

The no-decade symlink, abbreviation source-validation, explicit boundary, and streamed JSON parser findings remained closed.

## Review Commands

- Independent reviewer subagent over `main..HEAD`
- `timeout 300 node /Users/sungmin/.claude/plugins/cache/openai-codex/codex/1.0.4/scripts/codex-companion.mjs review --wait --base main --scope branch`
- `timeout 300 node /Users/sungmin/.claude/plugins/cache/openai-codex/codex/1.0.4/scripts/codex-companion.mjs adversarial-review --wait --base main --scope branch -`
