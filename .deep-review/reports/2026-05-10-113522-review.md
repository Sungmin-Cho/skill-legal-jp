# Deep Review Report — 2026-05-10

## Summary
- **Verdict**: REQUEST_CHANGES
- **Review Mode**: 3-way Cross-Model
- **Issues**: 🔴 0건, 🟡 2건, ℹ️ 0건

## Cross-Model Verification

| 항목 | Reviewer A | Reviewer B | Reviewer C | 확신도 |
|------|------------|------------|------------|--------|
| Duplicate `lawsuit_id` can map metadata to the wrong detail JSON | — | 🟡 | — | 중간 |
| Title/case-number search ignores detail JSON files absent from `list.json` | — | 🟡 | — | 중간 |
| No remaining issues | ✅ | — | ✅ | 중간 |

## Code Review

### 🔴 Critical

None.

### 🟡 Warning

1. Duplicate `lawsuit_id` can map multiple upstream records to the same JSON file.
   - Evidence: planned `build_lawsuit_index()` keys only by suffix at `docs/superpowers/plans/2026-05-10-legal-jp-dual-skill-mvp.md:695-703`; `discover_json_path()` resolves by `lawsuit_id` only around `:712-715`
   - Fix: track all paths per `lawsuit_id` and disambiguate by `trial_type`, case number, and court.

2. Metadata/title/case-number search ignores detail JSON files that are not represented in `list.json`.
   - Evidence: planned `metadata_entries()` iterates only `list.json` rows around `docs/superpowers/plans/2026-05-10-legal-jp-dual-skill-mvp.md:747-762`; title/case-number modes use only that metadata path around `:858-861`
   - Fix: append unmatched detail JSON files as searchable entries after processing `list.json`.

### 🟢 Passed

- Two reviewers approved the current plan.
- Structured date preservation and previous schema/performance fixes were confirmed.

