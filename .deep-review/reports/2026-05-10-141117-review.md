# Deep Review Report — legal-jp edge-case re-review

Generated: 2026-05-10 14:11:17 KST

## Scope

- Base: `main` (`d4430bed6ccd24117c6f404ccfffcc3454805d76`)
- Head before response: `93c1a33`
- Review target: full branch diff `main..HEAD`

## Reviewers

1. Independent implementation reviewer: `REQUEST_CHANGES`
2. Codex companion review: pending/no final verdict captured before response
3. Codex companion adversarial review: `REQUEST_CHANGES`

## Verdict

`REQUEST_CHANGES`

## Findings

### P1 — Streamed `ryakusyou.json` directory escaped fail-closed handling

Source: independent implementation reviewer.

Evidence:
- `.claude/skills/legal-jp/scripts/search_law.py` converted `PermissionError` from `stream_json_array()` into `DataFileError`, but other `OSError` cases such as `IsADirectoryError` escaped.
- `main()` only converts `DataFileError` into stdout `[]` and exit code `2`, so this path produced traceback-style output and exit code `1`.

Impact:
- Required abbreviation data could violate the CLI error contract when the file path existed but was not an openable JSON file.

### P1 — Title search eagerly loaded unrelated precedent details before matching

Source: Codex companion adversarial review.

Evidence:
- `.claude/skills/legal-jp/scripts/search_precedent.py` called `iter_metadata_entries(..., load_details=True)` for title metadata search.
- That caused detail JSON for list rows to be loaded before confirming the row matched the title query.

Impact:
- A malformed or unreadable unrelated detail file could block a valid later title match.

### P2 — Abbreviation usage evidence was deduplicated only by law number

Source: Codex companion adversarial review.

Evidence:
- `.claude/skills/legal-jp/scripts/search_law.py` used a single `seen` set keyed by `result.get("num")`.
- `abbreviation_usage` rows in `ryakusyou.json` can represent article/chapter-granularity evidence inside the same law number.

Impact:
- Multiple distinct unresolved abbreviation usages in the same law could collapse to one result.

## Review Commands

- Independent reviewer subagent over `main..HEAD`
- Codex companion review over `main..HEAD`
- Codex companion adversarial review over `main..HEAD`
