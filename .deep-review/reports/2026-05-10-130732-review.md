# Deep Review Report — legal-jp lookup hardening re-review

Generated: 2026-05-10 13:07:32 KST

## Scope

- Base: `main` (`d4430bed6ccd24117c6f404ccfffcc3454805d76`)
- Head before response: `5a19c92`
- Review target: full branch diff `main..HEAD`

## Reviewers

1. Independent implementation reviewer: `REQUEST_CHANGES`
2. Codex companion review: `REQUEST_CHANGES`
3. Codex companion adversarial review: `REQUEST_CHANGES`

## Verdict

`REQUEST_CHANGES`

## Findings

### P1 — Implicit no-decade precedent search could cross the dataset boundary

Source: independent reviewer and Codex companion review.

Evidence:
- `.claude/skills/legal-jp/scripts/search_precedent.py` validated explicit `--decade`, but the implicit no-decade branch still accepted any `precedent/*` directory where `Path.is_dir()` returned true.
- A symlinked directory such as `precedent/2030 -> /tmp/outside_decade` was followed and its `list.json` could be searched as precedent metadata.

Impact:
- Default precedent searches without `--decade` could return evidence from outside the configured Japanese-law dataset.

### P2 — Abbreviation search could skip later required source validation

Source: Codex companion review.

Evidence:
- `.claude/skills/legal-jp/scripts/search_law.py` returned as soon as exact abbreviation matches filled `--limit`.
- With `--abbr 民法 --limit 1`, the script could return a successful result before opening later required abbreviation sources such as `law_abb.json` or `ryakusyou.json`.

Impact:
- A corrupt or missing required abbreviation file could be hidden by a small result limit, making the skill appear to have complete abbreviation coverage when source data was invalid.

### P1 — Case-number lookup missed common Japanese citation forms

Source: Codex companion adversarial review.

Evidence:
- Exact dataset form `令和5(受)604` matched.
- Common citation forms such as `令和５年（受）第６０４号` and `令和5（受）604` returned no results because the matcher only used raw casefold substring comparison.

Impact:
- A normal Japanese legal citation style could fail to retrieve the matching Supreme Court precedent.

### P2 — No-decade case-number search eagerly loaded every detail JSON

Source: Codex companion adversarial review.

Evidence:
- Case-number search iterated metadata entries and loaded detail JSON before testing whether the case-number field matched.
- The documented no-decade lookup path therefore paid detail-loading cost for unrelated precedents.

Impact:
- Common case-number searches were slower than necessary and more exposed to unrelated malformed detail files.

## Prior Findings Check

The earlier law JSON fail-closed, streamed abbreviation parser, parser separator, and explicit precedent path-boundary findings remained closed.

## Review Commands

- Independent reviewer subagent over `main..HEAD`
- `timeout 300 node /Users/sungmin/.claude/plugins/cache/openai-codex/codex/1.0.4/scripts/codex-companion.mjs review --wait --base main --scope branch`
- `timeout 300 node /Users/sungmin/.claude/plugins/cache/openai-codex/codex/1.0.4/scripts/codex-companion.mjs adversarial-review --wait --base main --scope branch -`
