# Deep Review Report — legal-jp precedent boundary re-review

Generated: 2026-05-10 12:59:41 KST

## Scope

- Base: `main` (`d4430bed6ccd24117c6f404ccfffcc3454805d76`)
- Head before response: `b75b33a`
- Review target: full branch diff `main..HEAD`

## Reviewers

1. Independent implementation reviewer: `REQUEST_CHANGES`
2. Codex companion review: no completed actionable report before timeout
3. Codex companion adversarial review: `REQUEST_CHANGES`

## Verdict

`REQUEST_CHANGES`

## Findings

### P1 — Precedent detail discovery could read outside the dataset through symlinks

Source: independent reviewer.

Evidence:
- `.claude/skills/legal-jp/scripts/search_precedent.py:72` indexed `*.json` detail paths under `precedent/<decade>` without resolving and checking containment.
- Discovered lawsuit-id and orphan-detail paths were later loaded without the explicit-path `ensure_within()` guard.

Impact:
- A symlink named like a precedent detail file could point outside `data_set` and be returned as local precedent evidence.

### P1 — `--decade` could escape `precedent/` and search non-precedent JSON

Source: Codex adversarial.

Evidence:
- `.claude/skills/legal-jp/scripts/search_precedent.py:200-204` joined raw `--decade` under `repo/precedent` and checked only `is_dir()`.
- Reproduction: `--decade ../law` caused precedent search to read `data_set/law/list.json` as if it were precedent metadata.
- An explicit missing decade returned a clean empty result instead of a data error.

Impact:
- Search results could silently mix statutes into precedent evidence, or hide a typo/missing decade as no results.

## Prior Findings Check

The earlier parser and law-ranking findings remained closed.

## Review Commands

- Independent reviewer subagent over `main..HEAD`
- `timeout 300 node /Users/sungmin/.claude/plugins/cache/openai-codex/codex/1.0.4/scripts/codex-companion.mjs review --wait --base main --scope branch`
- `timeout 300 node /Users/sungmin/.claude/plugins/cache/openai-codex/codex/1.0.4/scripts/codex-companion.mjs adversarial-review --wait --base main --scope branch -`
