# Deep Review Report — 2026-05-10

## Summary
- **Verdict**: REQUEST_CHANGES
- **Review Mode**: 3-way Cross-Model
- **Issues**: 🔴 0건, 🟡 7건, ℹ️ 0건

## Cross-Model Verification

| 항목 | Reviewer A | Reviewer B | Reviewer C | 확신도 |
|------|------------|------------|------------|--------|
| Markdown nested code fences break generated docs | — | 🟡 | 🟡 | 높음 |
| Final validation allows empty real-data smoke results | 🟡 | — | 🟡 | 높음 |
| Required workflow smoke prompt missing | 🟡 | — | 🟡 | 높음 |
| Precedent list/detail join assumes `file` but upstream uses `lawsuit_id` | — | 🟡 | — | 중간 |
| `law_abb.json` dict keys are discarded | — | 🟡 | — | 중간 |
| Synthetic-only validation misses upstream schema drift | — | — | 🟡 | 중간 |
| `--title` / `--case-number` search falls back to unrelated metadata fields | — | — | 🟡 | 중간 |
| `ryakusyou.json` large-file search loads full JSON | 🟡 | — | 🟡 | 높음 |

## Code Review

### 🔴 Critical

None.

### 🟡 Warning

1. Markdown snippets use triple-backtick outer fences while containing nested triple-backtick code blocks, so generated README/AGENTS/SKILL content is not reliably copyable.
   - Evidence: `docs/superpowers/plans/2026-05-10-legal-jp-dual-skill-mvp.md:57`, `:77`, `:138`, `:158`, `:737`, `:771`, `:860`, `:865`
   - Fix: use four-backtick outer fences for Markdown blocks that contain nested fences.

2. Final smoke validation accepts empty law or precedent results.
   - Evidence: `docs/superpowers/plans/2026-05-10-legal-jp-dual-skill-mvp.md:940`, `:949`, `:951`, `:960`
   - Fix: require non-empty real-data results, or dynamically sample known records and assert returned fields.

3. Final validation does not run the required workflow smoke prompt.
   - Evidence: design requires it at `docs/superpowers/specs/2026-05-10-legal-jp-dual-skill-design.md:170`; plan proceeds from script smoke to status around `docs/superpowers/plans/2026-05-10-legal-jp-dual-skill-mvp.md:930-1007`
   - Fix: add a workflow prompt check that verifies local evidence citation, evidence/analysis separation, and disclaimer.

4. Precedent metadata/detail join assumes a `file` field, but current upstream `precedent/2020/list.json` entries use `lawsuit_id`.
   - Evidence: plan fixture at `docs/superpowers/plans/2026-05-10-legal-jp-dual-skill-mvp.md:465`; planned implementation at `:593`
   - Fix: discover detail JSON paths by `lawsuit_id` when explicit file/path fields are absent.

5. `law_abb.json` dict entries lose their top-level key.
   - Evidence: planned implementation converts dicts with `list(data.values())` at `docs/superpowers/plans/2026-05-10-legal-jp-dual-skill-mvp.md:363`
   - Fix: preserve dict keys as `container_key` and expose them in output.

6. `--title` and `--case-number` metadata search can match unrelated metadata fields.
   - Evidence: planned fallback `or keyword.lower() in as_text(entry).lower()` at `docs/superpowers/plans/2026-05-10-legal-jp-dual-skill-mvp.md:631`
   - Fix: restrict matching to the requested fields, or add an explicit broad metadata mode.

7. Abbreviation search loads large `ryakusyou.json` fully.
   - Evidence: planned `load_json(repo / rel)` at `docs/superpowers/plans/2026-05-10-legal-jp-dual-skill-mvp.md:362` and `law/ryakusyou.json` in the default abbreviation list at `:395`
   - Fix: search smaller sources first, stop once `--limit` is satisfied, and stream top-level array records for `ryakusyou.json`.

### 🟢 Passed

- Plan scope matches the approved repo-local dual-surface MVP and does not include Codex plugin packaging.
- The plan uses standard-library Python and includes unit tests for both scripts.
- The plan keeps `data_set/` runtime-local and ignored by git.

