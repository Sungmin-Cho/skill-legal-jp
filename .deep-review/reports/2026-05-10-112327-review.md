# Deep Review Report — 2026-05-10

## Summary
- **Verdict**: REQUEST_CHANGES
- **Review Mode**: 3-way Cross-Model
- **Issues**: 🔴 0건, 🟡 5건, ℹ️ 0건

## Cross-Model Verification

| 항목 | Reviewer A | Reviewer B | Reviewer C | 확신도 |
|------|------------|------------|------------|--------|
| `--case-number` omits discovered filename / `lawsuit_id` matching | 🟡 | — | — | 중간 |
| `--name 民法` can return a distractor before canonical `民法` | — | 🟡 | — | 중간 |
| `text_search()` can emit full detail JSON in `raw` without `--content` | — | 🟡 | — | 중간 |
| No-decade title search is too slow for examples/gates | — | 🟡 | — | 중간 |
| Law fixtures do not match upstream abbreviation/replacement schemas | — | 🟡 | — | 중간 |
| No remaining issues | — | — | ✅ | 낮음 |

## Code Review

### 🔴 Critical

None.

### 🟡 Warning

1. `--case-number` does not search discovered `json_path` or `lawsuit_id`, even though the design says case number or filename.
   - Evidence: `docs/superpowers/specs/2026-05-10-legal-jp-dual-skill-design.md:121`, `docs/superpowers/plans/2026-05-10-legal-jp-dual-skill-mvp.md:707`, `:811`
   - Fix: include `json_path` and `lawsuit_id` in case-number search fields and add a test.

2. `--name 民法` can return an earlier substring match before canonical `民法`.
   - Evidence: `docs/superpowers/plans/2026-05-10-legal-jp-dual-skill-mvp.md:431`, `:1065`, `:1096`
   - Fix: rank exact normalized law-name matches before substring matches and add a distractor fixture.

3. `text_search()` can leak full precedent `contents` through `raw` when `--content` is false.
   - Evidence: `docs/superpowers/plans/2026-05-10-legal-jp-dual-skill-mvp.md:727`, `:777`
   - Fix: keep detail JSON separate for snippet/title extraction and strip heavy fields from `raw`.

4. No-decade `--title 損害賠償` examples and gates can scan all decades and repeatedly glob detail files.
   - Evidence: `docs/superpowers/plans/2026-05-10-legal-jp-dual-skill-mvp.md:930`, `:1076`
   - Fix: add `--decade 2020` to title examples/gates, build a per-decade lawsuit-id index once, and add a timeout to integration validation.

5. Law test fixtures do not match upstream `egov_abb.json`, `ryakusyou.json`, and `yomikae.json` shapes.
   - Evidence: `docs/superpowers/plans/2026-05-10-legal-jp-dual-skill-mvp.md:220-232`
   - Fix: use upstream-shaped fixture keys (`abbs`, `ryakusyou_lst`, `data`) and expose useful top-level fields in output.

### 🟢 Passed

- One reviewer approved after materializing embedded scripts/tests and running the planned test suites.
- Prior Markdown fence and empty-result validation issues are resolved.

