# Deep Review Report — legal-jp precedent fail-closed re-review

Generated: 2026-05-10 14:17:01 KST

## Scope

- Base: `main` (`d4430bed6ccd24117c6f404ccfffcc3454805d76`)
- Head before response: `fdb97e5`
- Review target: full branch diff `main..HEAD`

## Reviewers

1. Independent implementation reviewer: `CONCERN`
2. Codex companion review: `REQUEST_CHANGES`
3. Codex companion adversarial review: `REQUEST_CHANGES`

## Verdict

`REQUEST_CHANGES`

## Findings

### P2 — Precedent JSON open errors could escape fail-closed handling

Sources: independent implementation reviewer, Codex companion review.

Evidence:
- `.claude/skills/legal-jp/scripts/search_precedent.py` converted `PermissionError` from `load_json()` into `DataFileError`, but not broader `OSError` cases such as `IsADirectoryError`.
- A corrupted dataset where `precedent/<decade>/list.json` is a directory could exit with traceback-style output instead of stdout `[]` and exit code `2`.

Impact:
- The precedent CLI could violate the same data-error contract already enforced by the law search CLI.

### P1 — Title search could hide detail read failures behind successful partial results

Source: Codex companion adversarial review.

Evidence:
- `.claude/skills/legal-jp/scripts/search_precedent.py` stored `DataFileError` values from detail loading in `skipped_detail_errors`.
- It only raised a stored error if no results were found, so a corrupt detail encountered during title/detail scanning could be silently omitted when another result existed.

Impact:
- A legal evidence lookup could return exit code `0` and an incomplete result set while hiding dataset corruption.

## Review Commands

- Independent reviewer subagent over `main..HEAD`
- `timeout 600 node /Users/sungmin/.claude/plugins/cache/openai-codex/codex/1.0.4/scripts/codex-companion.mjs review --wait --base main --scope branch`
- `timeout 600 node /Users/sungmin/.claude/plugins/cache/openai-codex/codex/1.0.4/scripts/codex-companion.mjs adversarial-review --wait --base main --scope branch -`
